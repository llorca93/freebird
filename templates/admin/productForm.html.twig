{% extends "baseAdmin.html.twig" %}

{% block title %}produits - formulaire {% endblock %} 

{% block body %} 

    <h1>Formulaire - produits</h1>

    <div class="py-5">
    {{ form_start(productForm) }}
    {{ form_end(productForm) }}
    </div>

    <div class="text-center pb-5">
      <a href="{{ path('admin_product') }}" class="btn btn-danger">Annuler</a>
    </div>

{% endblock %}